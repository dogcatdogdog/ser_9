# 一种基于载重-能耗耦合等效电量距离变换的无人机多目标路径规划方法

> **专利申请技术交底书**
>
> 申请人：\_\_\_\_\_\_\_\_\_\_　　发明人：\_\_\_\_\_\_\_\_\_\_
> 第一发明人联系电话：\_\_\_\_\_\_\_\_\_\_　　所在单位/学院：\_\_\_\_\_\_\_\_\_\_
>
> 说明：本交底书用于向专利代理师说明技术方案，便于撰写专利申请文件。
> 正式提交前请确保相关论文/成果尚未公开见刊。

---

## 一、发明名称

**一种基于载重-能耗耦合等效电量距离变换的无人机多目标路径规划方法**

英文名称（供参考）：A Multi-Target Path Planning Method for Unmanned Aerial Vehicles
Based on Load-Energy Coupled Equivalent Energy Distance Transformation

名称说明：本发明属于方法类发明，名称中的"等效电量距离变换"指后文第 5.2 节定义的
距离变换步骤，"载重-能耗耦合"指无人机单位距离能耗率随载重线性变化的特性。

---

## 二、技术领域

本发明涉及无人机任务调度与路径规划技术领域，具体涉及一种在载重上限与电池续航联合约束下、
为单架无人机规划多目标点访问顺序（即旅行商问题/车辆路径问题型问题）的方法，
尤其涉及将载重对能耗的影响纳入路径优化目标、并保证所得路线在真实能量模型下可行的求解方法。

---

## 三、背景技术

### 3.1 现有技术现状

无人机执行末端配送、电力/管线巡检等任务时，通常需要从仓库（或基地）出发，
依次访问 N 个投递点或巡检点后返回。在单架无人机、目标点数量不大的情形下，
该问题在数学上属于典型的旅行商问题（TSP）或车辆路径问题（VRP）家族：
目标是找到一条访问顺序，使某个代价指标最小。

现有求解技术普遍采用以下两类做法：

（1）**以几何距离为目标的经典启发式**：最近邻构造（Nearest Neighbor）、
Clarke-Wright 节约算法（Savings）、2-opt、Or-opt 等，以及在此基础上形成的变邻域搜索（VND）。
这类方法的代价指标是目标点之间的**几何距离**之和。

（2）**通用最优化求解器**：OR-Tools（含 CP-SAT 精确求解）、PyVRP 等成熟工具，
通常也以几何距离（或行驶时间）为目标求解 TSP/VRP。

在上述两类做法中，无人机的载重上限与电池续航通常被作为**约束**处理，
即在求得几何最优（或近似最优）路线之后，再对该路线进行载重与电量校验；
校验不通过则判定该路线不可行。

### 3.2 最接近的现有技术及其不足

与本发明最接近的现有技术有两类。

**第一类是上述以几何距离为目标、可行性事后校验的路线规划方法。** 其不足在于：
无人机的单位距离能耗率并非恒定，而是随所载货物重量增加而增大（详见 3.3 节）。
因此"几何距离最短"与"总能耗最小"是两个不同的目标；
仅按几何距离优化得到的访问顺序，其实际能耗可能明显高于另一条几何距离略长但能耗更低的顺序。
更严重的是，由于可行性是**事后**校验的，该方法可能输出一条"几何上最优、但在真实能量模型下不可行"的
路线——即无人机将因电量耗尽而无法完成全部投递。

**第二类是近年来将能耗（或航程）与载重建立线性关系的路线规划方案。**
这类方案已经认识到载重会影响无人机的能耗或最大航程。
例如，公开号为 CN113706081B 的中国发明专利公开了"航程随载重线性递减"的模型
并引入载重惩罚系数；公开号为 CN116757585A 的中国发明专利公开了
单位距离功率随包裹重量变化的计算公式，并以"功率×距离/速度"计算总能耗；
公开号 CN122335152A 的申请则引入载荷增重模型进而修正飞行功率。

需要说明的是，**本发明不主张"载重与能耗（或航程）之间存在线性关系"本身**，
该关系属本领域已知技术。本发明与该类方案的区别在于**处理该关系的方式**：

（1）该类方案的边权（或功率项）与**访问状态无关**，因此必须建立显式的整数（非线性）规划模型，
把载重作为决策变量或约束写入模型，再依赖遗传算法、大邻域搜索（ALNS）、
群体智能等**大规模元启发式**算法求解；
（2）其求解过程计算量大、迭代次数多、结果依赖算法随机性，
难以在机载计算单元或指挥中心的在线场景中做到"毫秒级、确定性地重算"；
而这恰恰是小规模（目标点数在 20 以内）实时任务规划的常见需求。

（另需注意：大邻域搜索等方法本身也采用增量式的成本计算，
本发明的增量校验并非该理念的首创，详见附录 1 的说明。）

### 3.3 缺陷产生的原因

以第一个实测实例说明。设 8 个投递点构成同一个访问回路（即访问点集合与相邻关系完全相同），
仅将行进方向取反，两条路线满足：

- 总几何距离**完全相同**，均为 3962.2 m；
- 但在真实能量模型下，总能耗分别为 **872.7 Wh 与 827.1 Wh，后者较前者节省 5.2%**。

可见，**纯几何距离指标无法区分这两条路线**，而能耗差异真实存在。原因在于：
无人机在离开某点时的载重等于"尚未投递的全部货物重量之和"，
而按本说明书的能量模型（3.4 节），单位距离能耗率随该载重线性增长。
访问顺序改变后，**每一段航程的出发载重都随之改变**，
于是即便几何路径相同，总能耗也不同。

由此可知，现有技术的缺陷根源在于**优化目标与可行性判定相互分离**：

（1）**优化目标不含状态相关能耗**。由于每段航程的能耗取决于"出发时的载重"，
而载重又由"尚未访问的点集"决定，能耗本质上是一个**状态相关**的量；
以固定几何距离矩阵为目标的求解方法无法表达这一依赖关系。

（2）**可行性判定后置**。载重与电量被作为求得路线之后的检查项，
因此求解器给出的"最优解"可能根本不可行。实测中确有两类反例（均限定于下列具体实例配置）：

其一（**载重约束**的情形）：以**默认需求量**（每点 1~10 kg）、圆形分布、尺度 1000 m、
随机种子 42 生成的 10 点实例，总需求量达 **63.8 kg**，超过标准机型 50 kg 的载重上限。
此时任何访问顺序均不可行，正确表现是判定不可行并给出原因；
该实例说明的是**载重预检**的必要性——若缺少该预检，
求解器仍会输出一条"几何最优"的路线而不加提示。
（该实例的生成配置为：圆形分布、10 点、尺度 1000 m、随机种子 42、需求量区间 1~10 kg；
可复现脚本见 8.6 节所列 `scripts/gen_overload_example.py`。
该实例不在 8.2 节四组共 93 个实例的配置内，系为说明载重预检而单独构造。）

其二，在**电量紧张场景**（圆形分布、尺度 3000 m、电池容量 3500 Wh、共 10 个随机种子）下，
求解器返回的几何最优路线在 1 个实例（种子 9）上**电池耗尽**而不可行。
进一步核查该实例可发现：几何最优**访问回路**存在一个可行方向
（等效电量距离 34979.2 m，占该机型等效距离预算的 99.94%），
说明该实例并不缺少可行解，而是**"以几何距离为目标"的求解方式无法区分回路方向**，
从而可能返回不可行的那个方向。
此外，作为另一对照的通用求解器基线（PyVRP，同样以几何/时间目标求解）
在 10 个实例中有 2 个（种子 4、7）电池耗尽，而本方法在这 2 个实例上均给出可行解。

（3）**上述第二类方案的求解代价与本场景不匹配**。显式整数规划加元启发式的求解方式，
虽然可以表达载重-能耗关系，但其计算代价不适合"目标点数 20 以内、
需要在线快速重算、且要求结果确定可复现"的使用场景。

### 3.4 本说明书采用的能量模型

本发明采用载重-能耗线性耦合模型：单位距离能耗率

$$\rho(L)=\alpha+\beta L$$

其中 $L$ 为出发时的载重（kg），$\alpha$ 为空载能耗率（Wh/m，即纵截距），
$\beta$ 为载重敏感系数（Wh/(m·kg)，即斜率）。相应地，一段几何距离为 $d$ 的航程，
其能耗为 $d(\alpha+\beta L)$。

---

## 四、本发明要解决的技术问题

针对第 3.3 节指出的缺陷，本发明要解决的技术问题如下：

（1）**使载重-能耗耦合进入优化目标，而非仅作为事后校验**。
需要一种能够把"每段航程的能耗随出发载重变化"这一状态相关关系
纳入访问顺序优化过程的方法，使求解结果朝向"总能耗最小"而非"几何距离最短"。

（2）**使求解过程本身处理两类硬约束，而非事后校验**。
需要把载重上限与电池续航（含逐点返航电量预留）的判定
嵌入构造与局部搜索的每一步，
从而避免输出"按几何距离最优、但在真实能量模型下电量耗尽或载重超限"的路线。
（注：当总需求量本身超过载重上限时，任何访问顺序均不可行，
此时正确的技术表现是判定不可行并给出原因——本发明亦如此处理，见 5.3 节步骤 2。）

（3）**保持轻量与确定性**。
需要一种不依赖大规模元启发式的求解方式，使其在目标点数 20 以内的场景下
达到毫秒级求解，且对相同输入给出相同输出（结果可复现、可审计）。

---

## 五、本发明创造的技术手段

### 5.1 总体方案与符号说明

本发明的核心思路是：**把"每段能耗随出发载重变化"这一状态相关代价，
统一表示为一种随载重缩放的边代价（等效电量距离），
并在该边代价的定义下，用最近邻构造与 2-opt/Or-opt 局部搜索直接完成访问顺序的优化；
同时，在构造与局部搜索的每一步中执行电量可行性校验**。

**须如实说明该边代价的性质**（此点关系到本发明的技术贡献所在）：
由于实际能耗 $d(\alpha+\beta L)$ 与等效电量距离 $\mathrm{EED}=d(\alpha+\beta L)/\alpha$
之间仅相差机型常数因子 $\alpha$，
"以总等效电量距离最小为目标"与"以总能耗最小为目标"在数学上等价
（可行解集合与最优解均相同）。
因此，**等效电量距离的意义不在于改变优化问题的解，而在于其表达形式**：
它把状态相关的能耗改写为可与几何距离直接比较、叠加的"距离"量纲，
使 5.3、5.4 节的构造与搜索算子可以在同一量纲下统一处理代价与可行性判断。
**本发明不主张该变换本身带来了新的最优解**；
本发明的技术贡献在于下面三点构成的整体方案（详见 5.2~5.4 节及附录 1 的说明）：
①在该边代价下按"逐点返航电量预留"构造可行初始解；
②在局部搜索中以"受影响段"界定增量校验范围；
③上述两者与多起点确定性构造的配合，使方法无需依赖整数规划模型与元启发式框架，
即可在目标点数 20 以内以毫秒级、可复现地给出满足两类硬约束的解
（**当问题存在可行解时**；本方法为启发式构造，在电量接近饱和的个别实例上
可能未能搜索到已存在的可行解，见 6.4 节）。

方法总体流程如图 1 所示，共四个步骤：

1. **S1 等效电量距离变换**：构造载重感知的等效电量距离标度（各边权按出发载重逐段计算）；
2. **S2 电量感知构造**：以等效电量距离为依据，构造满足载重与往返电量约束的初始访问顺序；
3. **S3 增量式局部搜索**：以 2-opt 与 Or-opt 交替改进，每次候选移动仅对受影响段做增量校验；
4. **S4 全量后验证**：对最终顺序逐段模拟载重与能耗，输出结果与可行性。

本发明的优化目标为：在满足载重上限与电池续航约束的前提下，
**使访问序列的总等效电量距离最小**，即

$$\min_{\pi}\ \sum_{k=0}^{n} \mathrm{EED}\bigl(\pi_k,\pi_{k+1},L_k\bigr)$$

其中 $\pi_0=\pi_{n+1}=h$（仓库），$L_k$ 为访问完前 $k$ 个点后的剩余载重
（即第 $k$ 段出发时的载重，$L_k=\sum_{m>k} q_{\pi_m}$）。

由于 $\alpha$ 为正常数，而每段实际能耗等于该段等效电量距离乘以 $\alpha$
（见 5.2 节），故**最小化总等效电量距离等价于最小化总能耗**；
这一等价性使后续可以采用距离型算子进行求解。

符号说明如下表。

| 符号 | 含义 | 单位 |
|---|---|---|
| $n$ / $N$ | 目标点数量（本文场景 $N\le 20$） | — |
| $h$ / home | 仓库（基地）位置 | — |
| $t_i$ / $i$ | 第 $i$ 个目标点 | — |
| $d_{ij}$ | 目标点 $i,j$（或仓库与目标点）之间的几何距离 | m |
| $q_i$ | 目标点 $i$ 的需求量 | kg |
| $L$ | 离开某点时的载重（= 尚未投递需求之和） | kg |
| $\alpha$ | 空载能耗率（纵截距） | Wh/m |
| $\beta$ | 载重敏感系数（斜率） | Wh/(m·kg) |
| $C$ | 载重上限 | kg |
| $B$ | 电池容量 | Wh |
| $\mathrm{EED}(i,j,L)$ | 边 $i\to j$ 在出发载重 $L$ 下的等效电量距离 | m |
| $\pi$ | 访问顺序（目标点的一个排列） | — |

### 5.2 S1：等效电量距离变换

**输入**：目标点坐标与需求量、仓库坐标、机型参数 $\alpha$、$\beta$。
**处理**：对任意两点 $i$、$j$ 之间的航段，定义其在出发载重 $L$ 下的**等效电量距离**：

$$\mathrm{EED}(i,j,L)\;=\;d_{ij}\cdot\frac{\alpha+\beta L}{\alpha}\;=\;d_{ij}\Bigl(1+\frac{\beta}{\alpha}L\Bigr)$$

其物理含义是：该航段的实际能耗为 $\mathrm{EED}(i,j,L)\cdot\alpha$，
即等效电量距离乘以空载能耗率即得真实能耗（Wh）。
当 $L=0$ 时 $\mathrm{EED}=d_{ij}$，退化为几何距离，故本变换与几何距离相容。
**输出**：以等效电量距离为权重的航段代价。

图 2 给出能耗率随载重线性增长的关系（含纵截距 $\alpha$ 与斜率 $\beta$ 的标注），
图 3 给出等效电量距离的定义与"空载退化"性质。

**关于 $\mathrm{EED}$ 的性质**：由定义可见，$\mathrm{EED}$ 是 $(i,j,L)$ 的**三元函数**，
而**不是一个与访问顺序无关的固定矩阵**——这正是本问题区别于经典 TSP/VRP 的要点，
也是 3.2 节第二类方案必须显式建模的原因。

**本步骤的作用**：
等效电量距离把"每段能耗随载重变化"这一状态相关关系，
统一折算为一个**可比较、可叠加的距离标度**，
使构造与搜索过程可以用同一量纲处理边代价。
由于载重 $L$ 由"尚未访问的点集"确定，在构造与搜索中可依据当前已访问集合即时计算。

**须如实说明**：由于 $\mathrm{EED}$ 与能耗仅相差常数因子 $\alpha$，
该变换**不改变优化问题的解**；且因 $\mathrm{EED}$ 依赖访问状态（变量 $L$），
经典的基于**固定矩阵**的距离型算子**仍需按当前载重逐段求值**
（例如 5.4 节的增量评估、8.8 节节约算法的节约值计算，均须按位置对应的载重计算）。
本步骤的价值在于为后续步骤提供统一的代价表达，而非使算子无需改造。

需说明的是，本方法在构造与搜索中**一律使用精确的逐段载重**。
作为一种可选的加速实现，也可预先按近似载重 $\tau_i=$（总需求 − 点 $i$ 的需求量）
预生成一张等效距离矩阵用于粗筛；该近似不区分同一载重下的不同访问状态，
本实施例未采用，仅作为降低单次计算量的备选手段。

### 5.3 S2：电量感知的访问顺序构造

**输入**：目标点集合、仓库、机型参数、S1 的等效电量距离。
**处理**（按实施顺序）：

1. **输入校验**：目标点数量满足 $1\le N\le 20$；$\alpha>0$
   （保证等效电量距离定义有效）；
   （在服务化实现中，另对 $\beta$、载重上限、电池容量与各点需求量作非负性校验）。
2. **载重预检**：若所有目标点需求量之和超过载重上限 $C$，则直接判定不可行并返回，
   不再进行后续搜索。
3. **贪心构造（电量感知最近邻）**：自仓库出发，在每一步从未访问点中，
   选择满足下式且等效电量距离最小的点 $j$ 作为下一个访问点：

   $$B_{\text{cur}} \;\ge\; \alpha\cdot\mathrm{EED}(i,j,L_{\text{cur}})
     \;+\;\alpha\cdot\mathrm{EED}(j,h,L_{\text{cur}}-q_j)$$

   其中 $B_{\text{cur}}$ 为当前剩余电量，$i$ 为当前所在点，$L_{\text{cur}}$ 为离开 $i$ 时的载重，
   上式右端第一项为飞抵 $j$ 的能耗、第二项为由 $j$ 直接返航的能耗
   （**注意返航段的载重取"投递点 $j$ 之后"的载重 $L_{\text{cur}}-q_j$**）。
   该判定保证无人机到达任意一点后仍保留足够的返航电量。
4. **多起点取优（N 起点）**：分别以每一个目标点作为首个访问点各执行一次上述贪心构造，
   在所得各条可行路线中取总等效电量距离最小者。
   该最优初始解随后**只执行一次** 5.4 节的局部搜索（而非对 N 条各做搜索）。
5. **不可行处理**：若某一步不存在满足约束的未访问点，则该起点的构造终止，
   返回其已构造的部分路线，不进行回溯；若 N 个起点均为不可行，
   则返回其中**访问点数最多**的一条部分路线并标记为不可行。
   此时不再执行 5.4 节的局部搜索，直接输出。

**输出**：初始访问顺序及其总等效电量距离、可行性标记。

采用多起点的原因是：在 $N\le 20$ 的规模下，多起点构造的计算增量很小
（构造复杂度为 $O(N\cdot n^2)$），却能显著降低单起点贪心陷入较差局部解的概率。

### 5.4 S3：增量式局部搜索

**输入**：S2 的初始顺序。
**处理**：采用变邻域下降（VND）框架，以两种邻域**交替**改进，直到两个邻域均无改进
或达到预设迭代上限（默认 20 次外层迭代）：

**（a）2-opt 邻域**：翻转访问顺序中一段连续子路径。
即由 $\cdots\to a\to b\to c\to d\to e\to\cdots$
（$b,c,d$ 为被翻转的子段）
变为 $\cdots\to a\to d\to c\to b\to e\to\cdots$。

**（b）Or-opt 邻域**：将长度 1、2、3 的连续点子段整体剪下并插入到顺序中的其他位置。

对每一次候选移动，按下述方式判定：

1. **增量代价计算**：仅重算**受影响段**的等效电量距离增量。
   - **2-opt**：需要重算等效电量距离的范围为——两条被重新连接的边（$a\to d$、$b\to e$），
     以及被翻转子段 $b\to c\to d$ 内部的边（$b\to c$、$c\to d$ 两条；
     其出发载重因顺序倒转而改变）。子段之前（$\cdots\to a$）与之后（$e\to\cdots$）的边，
     其等效电量距离不重算。
   - **Or-opt**：需要重算的范围为——剪下处的前后两条新连接边、插入处的前后两条新连接边，
     以及被移动子段内部；因插入位置在剪下位置之前或之后时，
     子段及其中间点之后各段的出发载重会发生整体偏移，
     故范围为"剪下位置与插入位置之间"的全部边，加上被移动子段内部。
   上述范围之外的**边**一概不重算其等效电量距离。
   需要说明的是，本实施例在评估候选移动时，仍需从序列起点沿前缀推演一次
   当前位置的剩余电量与载重（见 5.6 节），故单次评估的实际计算量
   并不因上述范围限定而降低渐近阶数。
2. **可行性校验**：仅对受影响段校验电量——
   在受影响段上按段累计扣减电量，任意中间点的剩余电量不得为负。
   载重校验无需在增量评估中重复进行：由于构造阶段已作总需求预检，
   且各段出发载重随访问推进单调递减，
   故只要初始载重不超过载重上限，后续各段自动满足（本节末给出论证）。
3. **接受准则**：当且仅当增量代价小于零且可行性通过时接受该移动（首次改进策略），
   随后重新开始外层循环。

图 5 给出了 2-opt 翻转的示意，并用线型区分"不受影响段""被翻转子段内部"
与"被重新连接的边"。

**本步骤的技术效果（创新点 3）**：
由于能耗是状态相关量，朴素实现会对每次候选移动执行全量路线模拟；
本发明通过限定"受影响段"，使每次评估只需重算受影响范围内的少量边。
在相同的搜索过程与相同的结果质量下（实测 9 个实例的解完全一致），
增量方式相对全量方式平均提速约 3.0 倍（区间 2.5~3.4 倍，详见 6.4 节）。

> **数据口径说明**：上述 3.0 倍为实测加速比。需如实指出，
> 该对照中两版实现在数据结构与评估粒度上并不完全等价
> （全量版经由含对象构造与舍入的路线模拟器，增量版为纯浮点计算），
> 因此该倍数不宜单独归因于"受影响段"这一项限定；
> 且如 5.6 节所述，本限定不降低渐近复杂度，其收益属常数因子性质。

**载重约束自动满足的论证**：设构造阶段已通过总需求预检（$\sum q_i\le C$）。
访问过程中第 $k$ 段出发载重 $L_k=\sum_{m>k}q_{\pi_m}\le\sum_m q_m\le C$，
而 2-opt 与 Or-opt 均**不改变已访问集合**（仅改变访问顺序），
故任意中间点到任意位置，其"尚未投递需求量之和"始终不超过总需求，
即恒不超过载重上限。因此增量评估中只需校验电量。

### 5.5 S4：全量后验证与输出

**输入**：搜索得到的最优顺序。
**处理**：按顺序逐段模拟：
载重按各点需求量依次递减；每段能耗按该段出发载重由 $\rho(L)=\alpha+\beta L$ 计算；
逐段累计能耗并与电池容量比较（累计值超过电池容量即判定电量不可行）；
同时校验总需求量是否超过载重上限。
**输出**：包含访问顺序、总几何距离、总等效电量距离、总能耗、剩余电量、
总投递量、可行性标记与告警信息的路线规划结果。

注：返航电量的**逐点预留**在 5.3 节 S2 的构造与 5.4 节 S3 的增量校验中完成；
本步骤（S4）是对最终顺序的一次性全量确认。

后验证步骤的作用是：以精确的逐段模拟对快速搜索的结果做**最终确认**，
避免近似带来的误判。

### 5.6 复杂性与确定性分析

- **构造**：多起点贪心构造复杂度 $O(N\cdot n^2)$，$N=n$ 时为 $O(n^3)$，
  在 $n\le 20$ 时为数千次基本运算量级；
- **局部搜索**：单次候选移动只需评估**受影响段**的等效电量距离
  （2-opt 为 $O(|S|)+2$，$|S|$ 为翻转段长度）；
  由于构造与搜索需维护当前位置的剩余电量与载重，
  本实施例在每次评估时沿前缀累加该状态，故单次评估的实际代价为 $O(i+|S|)$；
  2-opt 邻域规模为 $O(n^2)$，故一轮完整 2-opt 扫描为 $O(n^3)$ 量级；
- **增量校验收益的性质说明**：由于 2-opt 全体候选的翻转段长度期望约为 $n/3$，
  "只算受影响段"相对"全量重算"在渐近阶数上并不降低复杂度，
  其实质收益是一个**常数因子**（实测约 3.0 倍，见 6.4 节）。
  本发明主张的是该**实现方式**与其带来的实测提速，而非阶数上的降低；
- **确定性**：方法本身不含随机过程（多起点构造为确定性遍历，
  局部搜索采用固定的邻域顺序与首次改进策略），
  故在**不启用求解时限**的配置下，相同输入必得相同输出，结果可复现、可审计。
  需说明：服务化实现为防止个别请求长时间占用资源，设有可配置的求解时限
  （见 8.7 节）；一旦该时限被触发而提前返回当前解，
  输出的具体结果将取决于机器负载，此时不保证逐位一致。

---

## 六、本发明创造的效果和优点

本章 6.2~6.4 节的数据在 8.2 节所列四组实例（共 93 个）上取得；
6.1 节的直观实例与 8.3 节的走通实例为**单独配置**的实例
（随机分布、8 点、尺度 800 m、种子 2，见 8.3 节），不计入上述 93 个。
除特别说明外，对照基线为"几何距离最优"路线（由精确求解获得）在**同一能量模型下**的能耗。

### 6.1 访问顺序对能耗的影响（直观实例）

8 点实例中，两条访问方向相反、**总几何距离同为 3962.2 m** 的路线，
其真实能耗分别为 **872.7 Wh 与 827.1 Wh**，后者较前者**节省 5.2%**
（该百分比以基线路线能耗为分母，与本章其余各表的口径一致）。
该实例（图 4）直接说明：几何距离指标无法区分这两条路线，
只有把载重-能耗耦合纳入目标，才能选出能耗更低的访问方向。

### 6.2 相对几何最优路线的省电幅度

实测结果如下（负值表示本方法能耗低于几何最优路线）：

| 实例组 | 规模 | 数量 | 能耗节省（均值±标准差） |
|---|---|---|---|
| 圆形分布（自建） | 5 点 | 10 | −1.66% ± 1.35% |
| 圆形分布（自建） | 10 点 | 10 | −1.69% ± 1.50% |
| 圆形分布（自建） | 15 点 | 10 | −3.06% ± 2.11% |
| 圆形分布（自建） | 20 点 | 10 | −2.34% ± 1.23% |
| 随机分布（自建） | 5 点 | 10 | −1.11% ± 2.02% |
| 随机分布（自建） | 10 点 | 10 | −4.60% ± 4.49% |
| 随机分布（自建） | 15 点 | 10 | −3.55% ± 6.02% |
| 随机分布（自建） | 20 点 | 10 | **−5.24% ± 5.05%** |
| 电量紧张（自建） | 10 点 | 9（注） | −1.98% ± 1.69% |
| Solomon 前 20 点 | 20 点 | 3 | −7.83%（R101 −5.2%、C101 −2.1%、**RC101 −16.2%**） |

> 注：电量紧张组共 10 个实例，其中 1 个（随机种子 9）本方法未搜索到可行解（见 6.4 节），
> 本表统计基于其余 9 个可行实例。

由上表可见：

（1）**随目标点数增加，省电幅度总体呈增大趋势**：
随机分布由 5 点的 −1.11% 增至 20 点的 −5.24%，10 点以上普遍获得 3.5%~5.2% 的节省，
20 点收益最大。需说明的是，该趋势**并非逐点单调**——
随机分布 15 点为 −3.55%（较 10 点回落，该组标准差达 ±6.02%），
圆形分布 20 点亦略低于 15 点，此为实例间差异所致，
故上述结论应理解为**区间趋势**而非逐点单调。
（2）在规则布置的圆形分布下，省电幅度较小（约 1.7%~3.1%），
原因是分布规则时几何最优与能耗最优的顺序差异本就有限。
（3）在标准算例（Solomon R101/C101/RC101 前 20 点）上，
本方法的能耗低于几何最优路线 2.1%~16.2%（均值 7.83%），
其中点分布较分散的 RC101 收益最大。
（4）在电量紧张场景下，本方法在节省能耗（−1.98%）的同时，
9 个实例保持可行（1 例未获可行解，见 6.4 节）。
需说明该组是本文各实例组中**唯一出现基线不可行**的场景：
其等效电量距离预算为 35000 m，多数实例用量达预算的 92% 以上（最高约 97.5%），
即电量约束在该组接近饱和（详见 8.4 节）。

**结论性表述建议**：目标点分布越分散、载重占比越高、规模越大，
载重-能耗耦合带来的优化空间越大，本方法的收益越显著（详见图 9）。

### 6.3 各技术特征的贡献（消融实验）

在同一实例组上分别禁用某一技术特征后重测总等效电量距离（完整方法记为 100%）：

| 变体 | 圆形 5 点 | 圆形 20 点 | 随机 5 点 | 随机 20 点 |
|---|---|---|---|---|
| 完整方法 | 100.0% | 100.0% | 100.0% | 100.0% |
| 仅最近邻构造（无局部搜索） | 100.4% | 100.1% | 101.5% | **105.2%** |
| 去 Or-opt | 100.3% | 100.1% | 100.0% | **103.1%** |
| 去 2-opt | 100.1% | 100.0% | 100.9% | 100.2% |
| 固定载重（忽略载重-能耗耦合） | 101.7% | 102.4% | 102.1% | **114.6%** |

可见：

（1）**忽略载重-能耗耦合造成的损失最大**（随机 20 点达 +14.6%），
且该损失**随规模增大而放大**（圆形分布由 5 点 +1.7% 增至 20 点 +2.4%，
随机分布由 5 点 +2.1% 增至 20 点 +14.6%），说明耦合建模的收益在分散分布与较大规模下最为突出；
（2）局部搜索的贡献次之。在随机 20 点规模上，Or-opt 的贡献大于 2-opt
（去 Or-opt +3.1%，去 2-opt 仅 +0.2%）；
但在小规模上二者互有胜负（随机 5 点：去 2-opt +0.9%、去 Or-opt 为 +0.0%，
方向相反），故此处只就 20 点规模给出结论。
（3）综合而言，三项技术特征的贡献主次为：**载重耦合建模 > 局部搜索 > 单一邻域算子**。

另需说明：本表列出 5 个变体；评测表中另有一个"无电池约束"变体，
在自建实例上其相对成本恰为 100.0%（因该组电量约束大多不绑定），故未列入表中。

相关趋势见图 8、图 6。

### 6.4 求解性能与可行性

（1）**求解速度**：单实例平均求解时间为
5 点 1.04 ms、10 点 8.19 ms、15 点 26.96 ms、20 点 92.05 ms，
即在目标点数 20 以内为**毫秒级**，满足在线重算需求（图 7）。
作为参照，同一批实例上 OR-Tools CP-SAT 精确求解几何 TSP 的耗时为
4.49 / 10.71 / 43.29 / 715.84 ms。
**该对照需注意其不完全可比性**：CP-SAT 求解的是几何距离目标并须**证明最优**，
而本方法是能量目标下的启发式，二者目标与终止条件均不同，
故上述数字仅用于说明量级差异，不作为严格的性能优势证据。

**本方法的几何代价（如实披露）**：以能耗为目标会使总几何距离有所增加。
实测本方法的几何距离相对几何最优路线：
Solomon 组 3 个实例分别为 +6.7%、+3.2%、+15.4%；
自建组 80 个实例平均 +1.0%（最大 +11.5%）。
即本方法是以**少量几何绕行**换取更低的实际能耗。

（2）**增量校验的效果**：在 9 个实例上对比增量评估与全量评估，
在解完全一致（成本比 100%）的前提下，增量方式平均提速 **3.0 倍**（区间 2.5~3.4 倍）。
该倍数为实测值，其口径与局限见 5.4 节的说明（对照实现的数据结构不完全等价，
且不降低渐近复杂度）。

（3）**可行性**：93 个实例中，本方法在 **92 个**实例上给出可行路线；
唯一例外为电量紧张组的种子 9——该实例中本方法在构造阶段未找到可行点，
返回部分路线并标记不可行（此时不进入局部搜索）。

对该例外须**如实说明**：经核查，该实例的几何最优**访问回路**存在可行方向
（等效电量距离 34979.2 m，占等效距离预算 99.94%），
而本方法未搜索到任何可行解。
即：**在该实例上本方法劣于几何最优基线**，这是本方法的已知局限——
当电量接近饱和（本例占用预算 99.94%）时，
基于贪心构造的启发式可能因早期选择不当而无法构造出可行解；
能量感知精确动态规划在该实例上可求得可行解（34497.1 m）。

对"避免输出不可行路线"这一效果，如实对照如下：

| 对照对象 | 该对照不可行的实例 | 本方法在这些实例上 |
|---|---|---|
| 几何最优（求解器返回的方向） | 种子 9（1 例） | **本方法同样不可行**（未体现优势） |
| 几何最优（并就可行方向择优后） | 0 例 | **本方法在种子 9 上劣于该基线** |
| 通用求解器基线 PyVRP | 种子 4、7（2 例） | 均给出可行解 |

故本节不作"零误判""避免不可行"之类的概括性主张：
本方法在可行性上相对几何最优基线**未见优势，且在 1 个极端实例上劣于该基线**；
其可行性方面的可对照收益仅存在于与通用求解器基线的比较中（2 例）。

### 6.5 本章小结

本方法在目标点数 20 以内的单机场景中，取得了以下效果（均如实限定）：

①相对几何最优路线更省电：随机分布 20 点 −5.24%、Solomon 最大 −16.2%；
代价是本方法的几何距离劣于几何最优（Solomon 组 3.2%~15.4%，自建组平均约 1%），
即以少量几何绕行换取更低的能耗；
②输出路线在真实能量模型下可行：93 个实例中 92 个可行；
相对**通用求解器基线**避免了 2 例不可行输出；
但**相对几何最优基线未见可行性优势**，且在 1 个电量近饱和实例上劣于该基线（见 6.4(3)）；
③求解为毫秒级，且（在未启用时限的默认配置下）结果确定、可复现。

---

## 七、附图及附图说明

本交底书附有下列 9 幅图，与正文的对应关系如下。

**图 1** 本发明方法的总体流程图（对应 5.1 节的 S1~S4 四个步骤）。

**图 2** 单位距离能耗率随载重线性增长的关系图
（对应 3.4 节，标注纵截距为空载能耗率 $\alpha$、斜率为载重敏感系数 $\beta$）。

**图 3** 等效电量距离的定义示意图（对应 5.2 节，示明满载出发时等效电量距离大于几何距离、
空载出发时退化为几何距离）。

**图 4** 访问顺序对能耗影响的实例图：（a）（b）为**同一个**几何最优访问回路的
**方向相反的两个访问方向**，二者总几何距离相同（均为 3962.2 m）；
（a）为几何最优方向、（b）为本方法所得方向。**图中编号 $k$（$k=1,\dots,8$）
对应第 8.3 节实施例中的目标点 $t_k$**；标有"仓库"的方块为仓库位置。
两方向的能耗分别为 872.7 Wh 与 827.1 Wh。

**图 5** 2-opt 翻转与受影响段示意图（对应 5.4 节）：图中 a、b、c、d、e
为访问顺序中相邻的五个点（标有"仓库"的方块为仓库位置），
上排为翻转前、下排为翻转后。图中字母为**顺序位置符号**（表示"被翻转段的前一点、
子段首点…"等相对位置关系），与 5.4 节正文的 a、b、c、d、e 一一对应。
图中线型的含义为：
**细灰实线**表示翻转前后不受影响的边（仓库—a、e—仓库）；
**虚线**表示被翻转子段 $b\to c\to d$ 的内部边；
**粗黑实线**表示被重新连接的边（翻转前的 a—b、d—e，翻转后的 a—d、b—e）。

**图 6** 消融实验柱状图（对应 6.3 节，20 点随机实例）。

**图 7** 求解耗时随目标点数变化的曲线图（对应 6.4 节）；
图中另有一条水平点线，为通用求解器基线在固定求解时限（2 秒）下的截断值，
非其收敛耗时，故不作为性能比较依据。

**图 8** 分组消融实验趋势图（对应 6.3 节，圆形与随机分布 × 5/10/15/20 点）。

**图 9** 能耗节省幅度图（对应 6.2 节：（a）按规模；（b）典型实例）。

> **图 6~9 的性质说明**：图 6~图 9 为实验数据图，用于呈现 6.2~6.4 节的效果数据。
> 正式申请时，若审查要求说明书附图不得仅为表示效果的附图，
> 可将图 6~图 9 的内容改以**实施例中的数据表**形式记载（相应数据已在 6.2~6.4 节列出），
> 附图仅保留图 1~图 5。

---

## 八、具体实施方式

### 8.1 机型参数

本发明的实施可采用下列三类机型参数（下表），
其中标准机型用于主要实施例，轻载与重载机型用于说明本方法对不同机型的通用性。

| 机型 | 载重上限 C | 电池容量 B | 空载能耗率 α | 载重敏感系数 β | 巡航速度 |
|---|---|---|---|---|---|
| light（轻载） | 5 kg | 1000 Wh | 0.15 Wh/m | 0.01 Wh/(m·kg) | 8 m/s |
| **standard（标准）** | **50 kg** | **5000 Wh** | **0.10 Wh/m** | **0.005 Wh/(m·kg)** | 10 m/s |
| heavy（重载） | 500 kg | 100000 Wh | 0.08 Wh/m | 0.002 Wh/(m·kg) | 12 m/s |

> 表中巡航速度不参与本方法的任何计算（本方法的能耗模型仅由距离与载重决定），
> 列出仅为说明机型参数来源。

**适用边界说明**：本方法的实施前提是单架无人机、目标点数不超过 20、
需求量与机型参数已知且静态、能耗率与载重为线性关系、不含时间窗约束。

### 8.2 实验数据与场景配置

实施例在下列**四组**实例上进行，共 93 个实例。
其中自建实例与电量紧张组的仓库设于坐标原点，Solomon 组沿用原算例库位 $(35,35)$：

| 实例组 | 数量 | 分布/来源 | 规模 | 尺度 | 需求量 | 机型 |
|---|---|---|---|---|---|---|
| 自建-随机分布 | 40 | 随机均匀分布 | 5/10/15/20 点 × 10 种子 | 1000 m | 1~3 kg | standard |
| 自建-圆形分布 | 40 | 圆周均匀分布 | 5/10/15/20 点 × 10 种子 | 1000 m | 1~3 kg | standard |
| 电量紧张 | 10 | 圆周分布 | 10 点 × 10 种子 | 3000 m | 1~3 kg | standard（电池改为 3500 Wh） |
| Solomon 标准算例 | 3 | R101/C101/RC101 前 20 点 | 20 点 | 原算例坐标 | 原算例需求 | heavy |

说明：
（1）自建实例的需求量取 1~3 kg，是为了使标准机型（载重上限 50 kg）
能够容纳 20 点实例的总需求（约 40 kg），从而将主要考察点集中在**能耗目标**上。
需说明：**载重约束绑定的情形**由 3.3 节的默认需求量实例覆盖（该组总需求约 40~60 kg
而电量紧张组总需求仅 21.0 kg，不可能使载重约束绑定）；
**电量约束绑定的情形**由"电量紧张"组覆盖。
（2）电量紧张组通过增大空间尺度（3000 m）并降低电池容量（3500 Wh），
构造出电池续航成为主要约束的场景。
（3）Solomon 为带时间窗的车辆路径问题（VRPTW）标准算例集，
此处截取其前 20 个客户点作为一般性算例（本方法不使用其中的时间窗信息）。

### 8.3 完整实施例（可重现）

以下给出一个完整实例的输入、求解过程与结果，用于说明本方法的可实施性。

**实例来源**：随机均匀分布，8 个目标点，尺度 800 m，随机种子 2，标准机型。

**输入数据**：

| 目标点 | x (m) | y (m) | 需求量 (kg) |
|---|---|---|---|
| t1 | −381.4 | −322.4 | 8.3 |
| t2 | −652.9 | 160.2 | 7.6 |
| t3 | −499.4 | −711.8 | 3.5 |
| t4 | 251.9 | 99.6 | 2.4 |
| t5 | −107.8 | 270.9 | 4.8 |
| t6 | 213.1 | 747.9 | 7.1 |
| t7 | −173.4 | −500.4 | 4.1 |
| t8 | 17.7 | 625.9 | 8.0 |

仓库位置：(0, 0)。总需求量 45.8 kg（≤ 50 kg，载重可行）。

**求解过程**：多起点构造在 8 个起点中选中以 $t_4$ 为首个访问点的路线为初始解；
随后局部搜索以 2-opt 与 Or-opt 交替改进，直至两个邻域均无改进。
为说明 5.4 节的增量校验，下面给出一处 2-opt 候选移动的评估示例：
初始解中相邻子序列为 $\cdots\to t_6\to t_8\to t_5\to t_2\to\cdots$，
考虑翻转子段（对应 5.4 节记法：$a=t_6$、$b=t_8$、$c=t_5$、$d=t_2$，本例子段含 $b$、$c$ 两点），
即由 $\cdots\to t_6\to t_8\to t_5\to t_2\to\cdots$ 变为 $\cdots\to t_6\to t_5\to t_8\to t_2\to\cdots$，
则只需重算三条受影响边 $t_6\to t_5$、$t_5\to t_8$、$t_8\to t_2$ 的等效电量距离
（出发载重分别为 43.4、36.3、31.5 kg，可由下表"出发载重"列按翻转后的顺序推得），
并校验该段累计电量不为负；
序列中 $t_6$ 之前与 $t_2$ 之后的部分不作重算。

**求解结果**：本方法给出的访问顺序为

> **t4 → t6 → t8 → t5 → t2 → t1 → t3 → t7**

逐段结果如下表（载重为出发时载重）：

| 航段 | 几何距离 (m) | 等效电量距离 (m) | 能耗 (Wh) | 出发载重 (kg) |
|---|---|---|---|---|
| 仓库→t4 | 270.9 | 891.2 | 89.12 | 45.8 |
| t4→t6 | 649.5 | 2058.8 | 205.88 | 43.4 |
| t6→t8 | 230.4 | 648.5 | 64.85 | 36.3 |
| t8→t5 | 376.5 | 909.3 | 90.93 | 28.3 |
| t5→t2 | 556.2 | 1209.8 | 120.98 | 23.5 |
| t2→t1 | 553.7 | 993.9 | 99.39 | 15.9 |
| t1→t3 | 406.9 | 561.5 | 56.15 | 7.6 |
| t3→t7 | 388.5 | 468.2 | 46.82 | 4.1 |
| t7→仓库 | 529.6 | 529.6 | 52.96 | 0 |
| **合计** | **3962.2** | **8270.8** | **827.08** | — |

**对照结果**：由几何 TSP 精确求解（Held-Karp 动态规划；该方法与 OR-Tools CP-SAT
在 n=5、n=10 的实例上已由单元测试逐例验证结果一致）得到的几何距离最优访问顺序为
t7 → t3 → t1 → t2 → t5 → t8 → t6 → t4（即本方法结果的反向），
其总几何距离同为 3962.2 m，但在同一能量模型下的总能耗为 **872.71 Wh**。
故本方法在该实例上**节省能耗 5.2%**（以基线能耗为分母），
且两条路线均满足载重与电量约束。该实例即图 4 所示实例
（附图中编号 $k$ 即本节点 $t_k$）。

### 8.4 电量约束绑定场景的完整实施例

8.3 节的实例中，总能耗（827.08 Wh）仅占电池容量（5000 Wh）的约 16.5%，
**电量约束并未绑定**，故该例未演示电量受限时的行为。为此另给出一例。

**实例来源**：圆形分布，10 个目标点，尺度 3000 m，随机种子 0，
机型为标准机型但电池容量改为 3500 Wh（即 8.2 节的"电量紧张"组）。
此时电池的等效电量距离预算为 $3500/0.1=35000$ m。

**输入数据**（目标点 $t_1\sim t_{10}$ 均匀分布于半径 3000 m 的圆周上）：

| 目标点 | $t_1$ | $t_2$ | $t_3$ | $t_4$ | $t_5$ | $t_6$ | $t_7$ | $t_8$ | $t_9$ | $t_{10}$ |
|---|---|---|---|---|---|---|---|---|---|---|
| 极角（°） | 0 | 36 | 72 | 108 | 144 | 180 | 216 | 252 | 288 | 324 |
| 需求量 (kg) | 2.3 | 1.5 | 1.1 | 1.0 | 2.6 | 2.8 | 2.2 | 2.5 | 2.1 | 2.9 |

总需求量 21.0 kg（≤ 50 kg）。仓库位于原点。

**求解结果**：本方法给出的访问顺序为（沿圆周反向依次访问）
$t_{10}\to t_9\to t_8\to t_7\to t_6\to t_5\to t_4\to t_3\to t_2\to t_1$，
逐段结果如下：

| 航段 | 几何 (m) | 等效 (m) | 能耗 (Wh) | 出发载重 (kg) | 出发电量 (Wh) | 到达电量 (Wh) |
|---|---|---|---|---|---|---|
| 仓库→t10 | 3000.1 | 6150.1 | 615.01 | 21.0 | 3500.0 | 2885.0 |
| t10→t9 | 1854.1 | 3532.1 | 353.20 | 18.1 | 2885.0 | 2531.8 |
| t9→t8 | 1854.2 | 3337.6 | 333.76 | 16.0 | 2531.8 | 2198.0 |
| t8→t7 | 1854.1 | 3105.6 | 310.56 | 13.5 | 2198.0 | 1887.5 |
| t7→t6 | 1854.1 | 2901.7 | 290.17 | 11.3 | 1887.5 | 1597.3 |
| t6→t5 | 1854.1 | 2642.1 | 264.21 | 8.5 | 1597.3 | 1333.1 |
| t5→t4 | 1854.1 | 2401.1 | 240.11 | 5.9 | 1333.1 | 1093.0 |
| t4→t3 | 1854.2 | 2308.5 | 230.85 | 4.9 | 1093.0 | 862.1 |
| t3→t2 | 1854.1 | 2206.4 | 220.64 | 3.8 | 862.1 | 641.5 |
| t2→t1 | 1854.1 | 2067.3 | 206.74 | 2.3 | 641.5 | 434.8 |
| t1→仓库 | 3000.0 | 3000.0 | 300.00 | 0 | 434.8 | 134.8 |
| **合计** | **22687.2** | **33652.4** | **3365.25** | — | — | 余 134.8 |

**对照结果**（**基线口径**：几何最优访问回路存在两个相反方向、二者能耗不同，
本节一律取其中**能耗更低**的方向作为基线，即 $t_4\to t_5\to\cdots\to t_3$；
第 6.2、6.4 节的统计则直接采用精确求解器返回的回路方向，二者口径不同，不宜直接互算）：
- 上述公平基线的等效电量距离为 34245.7 m（占预算 97.8%），
  本方法为 33652.4 m（占预算 96.1%），
  即本方法在**电量约束接近饱和**的情况下仍**节省能耗 1.73%**；
- 能量感知精确动态规划在该实例上的最优值为 33226.1 m，
  本方法（33652.4 m）与其相差 1.28%，且同样满足电量约束；
- 本实例全程各段出发电量均未出现负值（最低到达电量 134.8 Wh），
  即 5.3 节的逐点返航电量预留判据在该实例中始终得到满足
  （需说明：该实例中该判据未被触发为筛选条件，其作用体现在保证所构造路线的电量裕度）。

### 8.5 求解性能

在上述 93 个实例上的单实例平均求解耗时为：

| 目标点数 | 5 | 10 | 15 | 20 |
|---|---|---|---|---|
| 本方法 (ms) | 1.04 | 8.19 | 26.96 | 92.05 |
| CP-SAT 几何精确解 (ms) | 4.49 | 10.71 | 43.29 | 715.84 |

增量评估相对全量评估的加速比（9 个实例）：平均 3.0 倍，区间 2.5~3.4 倍，解完全一致。

### 8.6 复现步骤

1. 按 8.2 节配置生成实例（固定随机种子）；
   其中 8.3 节实施例的实例单独生成（随机均匀分布、8 点、尺度 800 m、
   随机种子 2、需求量取默认区间 1~10 kg、标准机型）；
2. 调用求解入口 `plan_multistop(targets, home, drone, seed)`：
   输入为目标点列表、仓库位置、机型参数（含 α、β、载重上限、电池容量）
   与随机种子参数（该参数为接口预留；本方法的求解过程本身为确定性，不使用随机数）；
   内部依次执行 S1~S4 并返回路线规划结果；
3. 对照基线由 CP-SAT 求解几何最优路线，并以同一能量模型评估其能耗；
4. 完整评测可一次性执行：

```bash
python -X utf8 a3_python/benchmark.py --points 5,10,15,20 --runs 10 --output results/
```

评测输出（结果表与原始 JSON）位于 `results/` 目录。
三个实施例对应的复现脚本为：
8.3 节实例 `scripts/gen_fig4.py`（输出 `fig4_data.json`）、
8.4 节实例 `scripts/gen_constrained_example.py`（输出 `constrained_example.json`）、
3.3 节载重超限实例 `scripts/gen_overload_example.py`（输出 `overload_example.json`）。

### 8.7 落地形态

本方法已实现为不依赖外部求解器的纯函数，并进一步实现为 HTTP 服务：
服务接收目标点、仓库与机型参数，返回访问顺序、总等效电量距离与可行性标记。
两种实现（实验用脚本实现与生产服务实现）采用相同的四个步骤：
其等效电量距离矩阵的逐元素交叉校验误差小于 $10^{-6}$；
访问顺序完全一致，逐段数值在两位小数的舍入容差（$0.02$）内一致。
二者的差别在于：服务化实现为防止资源被长时间占用，设有可配置的求解时限，
超时即返回当前已找到的最优解（实验用实现不设时限）。
因此在时限内完成求解时两版输出一致；若时限被触发，服务化实现的输出可能提前终止。

### 8.8 其他实施方式

（1）**构造环节的替代**：S2 中的多起点最近邻构造，可替换为改造后的
Clarke-Wright 节约算法——以等效电量距离计算点对 $(i,j)$ 的节约值

$$s(i,j)=\mathrm{EED}(i,h,L_P-q_i)+\mathrm{EED}(h,j,L_P)-\mathrm{EED}(i,j,L_P-q_i)$$

其中 $L_P$ 为总需求量（即自仓库出发时的载重）。
**注意**：该式在节约值初筛阶段使用 $L_P$ 与 $L_P-q_i$ 作为近似载重
（即上文 5.2 节的可选加速口径，不考虑同一载重下的访问状态差异）；
子路径合并后一律以逐段精确载重的模拟做可行性校验。
按节约值降序合并子路径（当 $i$ 位于某路线末尾、$j$ 位于另一路线开头时尝试合并）。
该替代方式与最近邻构造共用相同的等效电量距离与可行性判定。
在 6 个标准实例的对比中，最近邻构造所得总等效电量距离不大于节约算法，
故本实施例取最近邻构造为主，节约算法作为并列实施方式保留。

（2）**载重敏感系数为零的退化情形**：当 $\beta=0$ 时，等效电量距离退化为几何距离，
本方法退化为常规的几何距离最短路径规划，
可作为说明本方法与前案方法相容性的边界情形。

（3）**局部搜索邻域的扩展**：S3 中的邻域可扩展至 3-opt 或更大子段长度的 Or-opt，
其受影响段的分析与增量校验方式与 5.4 节相同，仅计算量相应增加。

---

## 附录：创造性评述（供专利代理师参考，不计入正文）

### 1. 与最接近现有技术的区别

**首先声明**：本发明**不主张**"载重与能耗（或航程）之间存在线性关系"本身——
该关系属已知技术，已有公开文献与专利（如 CN113706081B、CN116757585A、
CN122335152A）对其进行建模并用于无人机路径规划。
本发明的区别在于**处理该关系的方式**及其带来的技术效果。

检索到的相关方案多在**显式建模**的方向上展开：把载重作为决策变量或约束
写入混合整数（非线性）规划模型，再依赖遗传算法、大邻域搜索（ALNS）、
群体智能等元启发式求解。其共同点是：**模型的边权与访问状态无关**，
因此必须显式建模才能表达"载重随访问推进而变化"。

本发明的区别技术特征是一个**相互配合的整体方案**。
**须先排除不属于本发明的部分**：等效电量距离与实际能耗仅相差常数因子 $\alpha$，
是同一目标的不同书写形式，**不构成独立的区别技术特征**（见 5.1、5.2 节）。

本发明真正主张的区别特征是：

（1）**在该边代价定义下按"逐点返航电量预留"构造可行解**：
构造过程中每选一点，即校验"飞抵该点 + 由该点直接返航"的能耗不超过当前剩余电量，
使解在构造阶段即满足电量约束，而非事后校验。
其边代价须按位置对应的载重逐段求值（因 $\mathrm{EED}$ 依赖访问状态，
经典距离型算子**仍需按载重求值**，不能直接套用固定矩阵）；

（2）**在局部搜索中按"受影响段"界定增量电量校验的范围**：
每次候选移动只处理受影响范围内的边，避免全量路线重新模拟。
须说明的是：**增量式的成本计算并非本发明首创**——
大邻域搜索等元启发式本身即采用增量成本评估（如 CN122155064A 公开的方案）；
且如 5.6 节所述，该限定不降低渐近复杂度，其收益属常数因子性质（实测约 3.0 倍）。
本发明的区别在于把它与前述"逐点返航电量预留"的可行性判据**结合**使用；

（3）**以确定性多起点构造配合固定邻域顺序的首次改进搜索**，
使相同输入得到相同输出（在不启用求解时限的配置下），
并与上述（1）（2）配合，使方法无需依赖整数规划模型与元启发式框架。

上述（1）（2）（3）的组合，未被检索到的单一文献所公开。
建议在权利要求中将其作为**一个整体技术方案**主张，
而非拆分为彼此独立的特征；尤其不应把"等效电量距离变换"单独作为区别特征主张。

### 2. 建议主张的权利要求要点

- **独立权利要求（方法）**：接收目标点集合、仓库位置与无人机参数（含空载能耗率 $\alpha$、
  载重敏感系数 $\beta$、载重上限、电池容量）；按
  $\mathrm{EED}(i,j,L)=d_{ij}(\alpha+\beta L)/\alpha$ 构造随出发载重变化的等效电量距离；
  在载重上限与返航电量约束下构造访问顺序；**以总等效电量距离最小为准则**
  对该顺序执行局部搜索，且对候选移动仅按受影响段做增量式电量可行性校验（载重上限由构造前的总需求预检保证）；
  对最终顺序逐段模拟并输出可行路线。
- **从属权利要求**：多起点（N 起点）构造；
  2-opt 邻域下受影响段为两条重连边与翻转子段内部；
  Or-opt 邻域下受影响段为剪下与插入位置之间的边及被移动子段内部；
  变邻域交替与迭代上限；改造型节约算法构造；机型参数与线性能耗率模型的具体取值；
  输出结果中包含可行性标记与告警信息。
- **建议代理人一并撰写装置权项与存储介质权项**：
  §8.7 已描述本方法的服务化实现形态（接收参数、返回路线的计算装置），
  据此可支持装置与计算机可读存储介质两类权利要求。
