% fig6_ablation — 消融归因（有益效果量化）
% 用途: 交底书 图6, §六: 20 点随机实例消融, 相对总等效电量距离（完整方法=100%）
% 数据: results/table_20260819_105511.md 消融 random_20p 组 (cost mean):
%   完整方法 100.0 / 仅最近邻 105.2 / 去Or-opt 103.1 / 去2-opt 100.2 / 固定载重 114.6
%   (固定载重 = 忽略载重耦合: 构造+搜索用 beta=0, 真实 beta 评估)
% 规范: 全中文; 黑白(灰度填充); 去标题(见 FIGURES.md 图注)
% 编译: bash docs/patent/build.sh
\documentclass[tikz,border=8pt]{standalone}
\usepackage[fontset=windows]{ctex}
\usepackage{amsmath}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\begin{document}
\begin{tikzpicture}
\begin{axis}[
  width=14.5cm, height=8cm,
  ybar, bar width=0.5,
  xmin=0.4, xmax=5.6,
  xtick={1,2,3,4,5},
  xticklabels={完整方法, 仅最近邻, 去 Or-opt, 去 2-opt, 固定载重},
  x tick label style={font=\small},
  ylabel={相对总等效电量距离（\%）},
  ymin=97.5, ymax=118,
  ymajorgrids, grid style={gray!22},
  nodes near coords, every node near coord/.append style={font=\small, anchor=south},
  label style={font=\small}, tick label style={font=\small},
]
\addplot[draw=black!75, fill=gray!55] coordinates {
  (1,100) (2,105.2) (3,103.1) (4,100.2) (5,114.6)};
% 基准线：完整方法 = 100%
\draw[densely dashed, black!60] (axis cs:1,100) -- (axis cs:5,100);
\end{axis}
\end{tikzpicture}
\end{document}
