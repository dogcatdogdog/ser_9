# A3 · 多目标访问路线规划 — 需求规格说明书

> 版本: v1.0 | 日期: 2026-08-05

---

## 1. 一句话问题

一批快递投递点（或巡检塔），先去哪后去哪最省电、还能赶得上时间窗。

## 2. 场景

- **配送**：无人机从仓库出发，访问 N 个投递点，每个点有货物需求量，投递后载重减少
- **巡检**：无人机从基地出发，依次巡检 N 个输电塔/设备点

## 3. 问题定义

### 3.1 形式化

给定：
- **仓库** home = (x₀, y₀)
- **目标点集** T = {t₁, t₂, ..., tₙ}，每个 tᵢ = (xᵢ, yᵢ, demandᵢ, [time_windowᵢ])
- **无人机参数**:
  - 最大载重 capacity (kg)
  - 初始电量 battery (Wh)
  - 空载能耗率 α (Wh/m) — 单位距离耗电
  - 载重敏感系数 β (Wh/m/kg) — 每 kg 载重增加的单位距离耗电

求解：
- **访问序列** π = [π₁, π₂, ..., πₙ]（仓库的排列）
- 满足：载重约束（任何时刻累积载重 ≤ capacity）、电量约束（总等效距离 ≤ battery / α）
- 最小化：等效电量距离总和

### 3.2 载重-电量耦合模型

```
段 (i→j) 的等效电量距离:
  E(i→j) = geo_dist(i→j) × (α + β × load_before_departure(i)) / α

其中 load_before_departure(i) = sum(demand[k] for k from i to end of route)
即：在当前点出发时，还未投递的货物总重量
```

### 3.3 约束

| 约束 | 类型 | MVP 阶段 |
|------|------|----------|
| 载重上限 | 硬约束 | ✅ 必须 |
| 剩余电量 ≥ 返航距离 | 硬约束 | ✅ 必须 |
| 时间窗 [readyᵢ, dueᵢ] | 软约束→硬约束 | ⬜ 进阶 (W10+) |
| 单无人机 | — | ✅ 必须 |
| 所有点必须访问 | 硬约束 | ✅ 必须 |

---

## 4. MVP 边界（明确不做什么）

| 项目 | MVP (W1-W9) | 进阶 (W10+) |
|------|-------------|-------------|
| 目标点数 | ≤ 20 | 扩展到 50/100 |
| 无人机数 | 1 架 | 多架 (→D18 调度) |
| 电量模型 | 线性载重-耗电 | 分段线性 / 实测曲线 |
| 时间窗 | 不做 | ✅ 加入 |
| 动态重规划 | 不做 | — |
| 多基地 | 不做 | — |
| 取送货配对 | 不做 | — |
| 实时性 | 不要求 | — |

---

## 5. 功能需求

### F1: 等效距离矩阵构建
- 输入：几何距离矩阵 + 各点需求量 + α/β 参数
- 输出：等效电量距离矩阵
- **注意**：等效距离是状态依赖的，取决于出发点时的剩余载重

### F2: 构造启发式
- 最近邻：从仓库出发，每次选最近（等效距离）未访问点
- 电量感知的 Savings 算法 (Clarke-Wright 改造版)

### F3: 局部搜索改进
- 2-opt 线路内交换
- Or-opt 单点/多点重定位
- 每次邻域移动后**增量校验电量可行性**（不全量重算）

### F4: 对比评测
- vs OR-Tools 精确解 (≤15 点小规模)
- vs PyVRP 原始 VRPTW (无电量约束)
- 消融实验：去掉电量 / 去掉载重耦合

### F5: 单测覆盖
- 正例：5 个点、电量充裕 → 找到可行解
- 退化：3 个点、电量不足以覆盖 → 正确报告不可行
- 边界：1 个点、空载 vs 满载 → 等效距离差异正确

---

## 6. 非功能需求

| 需求 | 指标 |
|------|------|
| 纯函数 | ✅ 无 I/O、无全局状态、无网络调用 |
| 确定性 | `plan_multistop(..., seed=S)` 相同输入 + 相同 seed → 相同输出 |
| 性能 (Python) | ≤20 点求解 < 30s |
| 性能 (Rust) | ≤20 点求解 < 1s |
| 单测数量 | ≥ 10 (正例/退化/边界) |
| 代码行数 (Python) | 核心 ≤ 500 行 |

---

## 7. 评价指标

| 指标 | 计算方式 | 目标 |
|------|----------|------|
| 总等效距离 | sum(E(i→j) for each segment) | 最小化 |
| 电量可行性 | 所有段出发电量 ≥ 该段能耗 + 返航能耗 | 100% |
| 载重可行性 | 任何段出发载重 ≤ capacity | 100% |
| vs OR-Tools gap | (heuristic - optimal) / optimal × 100% | < 10% |
| 求解耗时 | wall-clock time | < 30s (20 点) |

---

## 8. 风险与降级

| 风险 | 概率 | 降级方案 |
|------|------|----------|
| 等效距离近似误差大 | 中 | 在 Route 级别加全量后验证，修正 false positive |
| PyVRP API 版本变化 | 低 | 锁定 0.13.x；或降级为纯手写 NN+2opt |
| 电量 + 载重无可行解 | 中 | 返回"不可行" + 原因（缺多少电量/哪个点超载） |
| Python/Rust 结果不一致 | 低 | W6 强制对齐：同输入两版输出 diff，误差进单测断言 |
